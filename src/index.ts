/*! index.ts */

export function add1000(x: number): number {
  let res: number = x;
  res += 1000;
  return res;
}

export function mult1000(x: number): number {
  let res: number = x;
  res *= 1000;
  return res;
}
